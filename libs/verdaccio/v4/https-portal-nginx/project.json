{
  "root": "libs/verdaccio/v4/https-portal-nginx",
  "sourceRoot": "libs/verdaccio/v4/https-portal-nginx/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/verdaccio/v4/https-portal-nginx/**/*.ts"]
      }
    },
    "up": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v4/https-portal-nginx/src",
        "commands": [
          "docker-compose --project-name v4-https-portal-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml up -d"
        ]
      }
    },
    "down": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v4/https-portal-nginx/src",
        "commands": [
          "docker-compose --project-name v4-https-portal-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml down --volumes --remove-orphans"
        ]
      }
    },
    "recreate": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v4/https-portal-nginx/src",
        "commands": [
          "docker-compose --project-name v4-https-portal-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml up --build -d"
        ]
      }
    },
    "force-recreate": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v4/https-portal-nginx/src",
        "commands": [
          "docker-compose --project-name v4-https-portal-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml up --build --force-recreate -d"
        ]
      }
    },
    "system-prune": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "commands": ["docker system prune -a --volumes -f"]
      }
    },
    "all-commands": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "nx run verdaccio-v4-https-portal-nginx:up --mode=nginx",
          "nx run verdaccio-v4-https-portal-nginx:down --mode=nginx",
          "nx run verdaccio-v4-https-portal-nginx:recreate --mode=nginx",
          "nx run verdaccio-v4-https-portal-nginx:force-recreate --mode=nginx",
          "nx run verdaccio-v4-https-portal-nginx:system-prune"
        ]
      }
    }
  },
  "tags": ["type:config", "scope:verdaccio"]
}
