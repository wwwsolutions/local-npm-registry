{
  "root": "libs/verdaccio/v6/reverse-proxy-apache",
  "sourceRoot": "libs/verdaccio/v6/reverse-proxy-apache/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/verdaccio/v6/reverse-proxy-apache/**/*.ts"]
      }
    },
    "up": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v6/reverse-proxy-apache/src",
        "commands": [
          "docker-compose --project-name v6-apache-verdaccio-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml up -d"
        ]
      }
    },
    "down": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v6/reverse-proxy-apache/src",
        "commands": [
          "docker-compose --project-name v6-apache-verdaccio-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml down --volumes --remove-orphans"
        ]
      }
    },
    "recreate": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v6/reverse-proxy-apache/src",
        "commands": [
          "docker-compose --project-name v6-apache-verdaccio-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml up --build -d"
        ]
      }
    },
    "force-recreate": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/verdaccio/v6/reverse-proxy-apache/src",
        "commands": [
          "docker-compose --project-name v6-apache-verdaccio-{args.mode} --verbose -f ./{args.mode}/docker-compose.yml up --build --force-recreate -d"
        ]
      }
    },
    "system-prune": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "commands": ["docker system prune -a --volumes -f"]
      }
    },
    "all-commands": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "nx run verdaccio-v6-reverse-proxy-apache:up --mode=apache2",
          "nx run verdaccio-v6-reverse-proxy-apache:down --mode=apache2",
          "nx run verdaccio-v6-reverse-proxy-apache:recreate --mode=apache2",
          "nx run verdaccio-v6-reverse-proxy-apache:force-recreate --mode=apache2",
          "nx run verdaccio-v6-reverse-proxy-apache:system-prune"
        ]
      }
    }
  },
  "tags": ["type:config", "scope:verdaccio"]
}
